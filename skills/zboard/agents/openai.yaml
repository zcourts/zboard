interface:
  display_name: "Zboard"
  short_description: "Coordinate live agents through a shared folder"
  default_prompt: "Use $zboard to check for cross-agent dependencies and coordinate with the responsible agent."
policy:
  allow_implicit_invocation: true
